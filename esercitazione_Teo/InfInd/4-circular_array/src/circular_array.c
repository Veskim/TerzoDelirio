/*

char buff[N];
int size;

void push_element(char x)
{
    if(size > 0)
    {
        for(int i=size; i>0; i--)
            buff[i] = buff[i-1]
    }
    buff[0] = x;
    size++;
}


char pop_element()
{
    char ret = buff[size-1];
    size--;
    return ret;
}
*/